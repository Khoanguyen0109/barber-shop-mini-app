import React from "react";
import { TDiscount } from "../types/discount";

type Props = {
  item: TDiscount;
};

function VoucherItem({ item }: Props) {
  return <div>VoucherItem</div>;
}

export default VoucherItem;
